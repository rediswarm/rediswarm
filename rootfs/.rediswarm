export REDISWARM_MODE="${REDISWARM_MODE:-redis}" # or sentinel
export REDISWARM_SLOT=${REDISWARM_SLOT:-1}
export REDISWARM_SLOT_RETRY=${REDISWARM_SLOT_RETRY:-10}
export REDISWARM_SLOT_TIMEOUT=${REDISWARM_SLOT_TIMEOUT:-30}
export REDISWARM_SECRET=${REDISWARM_SECRET:-redis-swarm}

export REDIS_PRIMARY_PORT=${REDIS_PRIMARY_PORT:-6379}
export REDIS_HOSTNAME_PREFIX=${REDIS_HOSTNAME_PREFIX:-redis-}
export REDIS_CONFIG_PATH=${REDIS_CONFIG_PATH:-/etc/redis/config}

export SENTINEL_HOSTNAME_PREFIX=${SENTINEL_HOSTNAME_PREFIX:-sentinel-}
export SENTINEL_QUORUM=${SENTINEL_QUORUM:-2}
