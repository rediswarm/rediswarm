protected-mode no
masterauth "${REDISWARM_SECRET}"
requirepass "${REDISWARM_SECRET}"

dir "/data"

# RDB
dbfilename "dump.rdb"

# AOF
appendonly yes
appendfilename "appendonly.aof"

# Redis replication configs
replica-announce-ip ${REDIS_HOSTNAME_PREFIX}${REDISWARM_SLOT}
${REDIS_SLAVEOF}
